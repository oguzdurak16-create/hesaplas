// All calculator pages are intended to be discoverable by search engines.
// Keep indexability centralized here so metadata and sitemap stay in sync.
export function isIndexableTool() {
  return true
}
